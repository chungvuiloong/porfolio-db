{
  "name": "porfolio-db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "remove": "rm -rf ../frontend/build && rm -rf ../backend/build",
    "build:ui": "npm run remove && cd ../frontend/ && npm run build && cp -r build ../backend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
